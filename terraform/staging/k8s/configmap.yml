apiVersion: v1
kind: ConfigMap
metadata:
  name: octa-app-config
  namespace: staging
data:
  APP_ENV: "staging"
  PORT: "8000"

  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  REDIS_DB: "0"

  LOG_LEVEL: "INFO"

  METRICS_ENABLED: "true"
  METRICS_PATH: "/metrics"

